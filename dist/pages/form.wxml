
	<view class="container">
        <checkbox-group bindchange="checkboxChange">
            <label class="label"  wx:for="{{ items }}" wx:key="{{ index }}">
                <checkbox class="scale-smaller" value="{{ item.name }}" checked="{{ item.checked }}"/>
                <text>{{ item.value }}</text>
            </label>
        </checkbox-group>
        <checkbox-group bindchange="checkboxChange">
            <view class="section-title">自定义checkbox</view>
            <block  wx:for="{{ items }}" wx:key="{{ index }}">
                
    <label class="{{ label }} _add0ebf" bindtap="$mycheckbox$change" data-index="{{ index }}" data-checked="{{ item.checked }}" data-com-index="{{undefined}}">
        <view class="checkbox _add0ebf">
            <checkbox value="{{ item.name }}" checked="{{ item.checked }}" class="_add0ebf"/>
            <view class="icon {{ $mycheckbox$checkedData[index]  ? 'checked' : '' }} _add0ebf">
                <icon size="20" type="success" class="_add0ebf"/>
            </view>
            <view  wx:if="{{ !$mycheckbox$showData[index] && item.checked  }}" class="icon checked _add0ebf">
                <icon size="20" type="success" class="_add0ebf"/>
            </view>
        </view>
        <text class="_add0ebf">{{ item.value }}</text>
    </label>
            </block>
        </checkbox-group>
        <radio-group class="radio-group" bindchange="radioChange">
            <label class="label"  wx:for="{{ items }}" wx:key="{{ index }}">
                <radio value="{{ item.name }}" checked="{{ item.checked }}" class="scale-smaller"/>
                <text>{{ item.value }}</text>
            </label>
        </radio-group>
        <switch color="#f50" class="scale-smaller" checked bindchange="switch1Change"/>
        <view class="section">
            <view class="section-title">城市选择器</view>
            
    <view class="sections _9ad8d32">
        <picker bindchange="$city$provinceChange" value="{{ $city$provinceIndex }}" range="{{ $city$province }}" range-key="{{ $city$name }}" class="_9ad8d32">
            <view class="picker _9ad8d32">
                省：{{ $city$provinceValue }}
            </view>
        </picker>
        <picker bindchange="$city$cityChange" value="{{ $city$cityIndex }}" range="{{ $city$city }}" range-key="{{ $city$name }}" class="_9ad8d32">
            <view class="picker _9ad8d32">
                城市：{{ $city$cityValue }}
            </view>
        </picker>
        <picker bindchange="$city$areaChange" value="{{ $city$areaIndex }}" range="{{ $city$area }}" range-key="{{ $city$name }}" class="_9ad8d32">
            <view class="picker _9ad8d32">
                区域：{{ $city$areaValue }}
            </view>
        </picker>
    </view>
        </view>
        <view class="section">
            <view class="section-title">城市选择器2</view>
            
    <view class="sections _c1b61a4">
        <view bindtap="$city2$pickerShow" class="_c1b61a4">{{ $city2$provinceValue }} {{ $city2$cityValue }} {{ $city2$areaValue }}</view>
        <view class="picker-bg _c1b61a4" style="display: {{ $city2$show ? 'block' : 'none' }};" catchtouchstart="$city2$pickerHide"/>
        <view class="picker-box _c1b61a4" style="transform: translateY({{ $city2$top }}px); display: {{ $city2$show ? 'block' : 'none' }};">
            <view class="btn-box _c1b61a4">
                <button class="cancel _c1b61a4" bindtap="$city2$pickerHide">取消</button>
                <button class="ok _c1b61a4" bindtap="$city2$pickerSure">确定</button>
            </view>
            <picker-view class="picker _c1b61a4" indicator-style="height: 40px; line-height: 40px; background: rgba(0,0,0,.02); color: #f50;" value="{{ $city2$value }}" bindchange="$city2$change" bindtouchstart="$city2$changeReset">
                <picker-view-column class="_c1b61a4">
                    <view  wx:for="{{ $city2$province }}" wx:key="{{ index }}" class="item _c1b61a4" data-id="{{ item[$city2$id] }}">{{ item[$city2$name] }}</view>
                </picker-view-column>
                <picker-view-column class="_c1b61a4">
                    <view  wx:for="{{ $city2$city }}" wx:key="{{ index }}" class="item _c1b61a4" data-id="{{ item[$city2$id] }}">{{ item[$city2$name]  }}</view>
                </picker-view-column>
                <picker-view-column class="_c1b61a4">
                    <view  wx:for="{{ $city2$area }}" wx:key="{{ index }}" class="item _c1b61a4" data-id="{{ item[$city2$id] }}">{{ item[$city2$name]  }}</view>
                </picker-view-column>
            </picker-view>
        </view>
    </view>
        </view>
        <view class="section">
            <view class="section-title">自定义日期选择器</view>
            
    <view class="sections _d79ec16">
        <view bindtap="$pdate$pickerShow" class="_d79ec16">{{ $pdate$year }}年{{ $pdate$month }}月{{ $pdate$day }}日</view>
        <view class="picker-bg _d79ec16" style="display: {{ $pdate$show ? 'block' : 'none' }};" catchtouchstart="$pdate$pickerHide"/>
        <view class="picker-box _d79ec16" style="transform: translateY({{ $pdate$top }}px); display: {{ $pdate$show ? 'block' : 'none' }};">
            <view class="btn-box _d79ec16">
                <button class="cancel _d79ec16" bindtap="$pdate$pickerHide">取消</button>
                <button class="ok _d79ec16" bindtap="$pdate$pickerSure">确定</button>
            </view>
            <picker-view class="picker _d79ec16" indicator-style="height: 40px; line-height: 40px; background: rgba(0,0,0,.02); color: #f50;" value="{{ $pdate$value }}" bindchange="$pdate$change" bindtouchstart="$pdate$changeReset">
                <picker-view-column class="_d79ec16">
                    <view  wx:for="{{ $pdate$years }}" wx:key="{{ index }}" class="item _d79ec16">{{ item }}年</view>
                </picker-view-column>
                <picker-view-column class="_d79ec16">
                    <view  wx:for="{{ $pdate$months }}" wx:key="{{ index }}" class="item _d79ec16">{{ item }}月</view>
                </picker-view-column>
                <picker-view-column class="_d79ec16">
                    <view  wx:for="{{ $pdate$days }}" wx:key="{{ index }}" class="item _d79ec16">{{ item }}日</view>
                </picker-view-column>
            </picker-view>
        </view>
    </view>
        </view>
        <view class="section">
            <view class="section-title">时间选择器</view>
            <picker mode="time" value="{{ time }}" start="09:01" end="21:01" bindchange=" timeChange ">
                <view class="picker">
                    当前选择: {{ time }}
                </view>
            </picker>
        </view>

        <view class="section">
            <view class="section-title">日期选择器</view>
            <picker mode="date" value="{{ date }}" start="2015-09-01" end="2017-09-01" bindchange="dateChange">
                <view class="picker">
                    当前选择: {{ date }}
                </view>
            </picker>
        </view>

        <view class="section">
            <text class="section__title">设置left/right icon</text>
            <view class="body-view">
                <slider class="scale-smaller" bindchange="sliderChange" activeColor="#f50" value="20" show-value/>
            </view>
        </view>
		<view class="section">
			<image wx:for="{{ files }}" wx:key="{{ index }}" style="width: 100px;" mode="widthFix" src="{{ item }}"/>
			<button class="choose" bindtap="chooseImage"><text>选择图片</text></button>
		</view>
        <view class="section">
            <textarea bindconfirm="textConfirm" placeholder="请输入" confirm-type="done"/>
        </view>

        <view><view style="width: 100%; height: 100px; background: #eee;"/></view>
		
    <view class="wetoast {{$toast$reveal ? 'wetoast_show' : ''}}">
        <view class="wetoast__mask"/>
        <view class="wetoast__bd" animation="{{$toast$animationData}}">
            <block  wx:if="{{$toast$img}}">
                <view class="wetoast__bd__img {{$toast$imgClassName || ''}}">
                    <image class="wetoast__bd__img_desc" src="{{$toast$img}}" mode="{{$toast$imgMode || 'scaleToFill'}}"/>
                </view>
            </block>
            <block  wx:if="{{$toast$title}}">
                <view class="wetoast__bd__title {{$toast$titleClassName || ''}}">{{$toast$title}}</view>
            </block>
        </view>
    </view>

	</view>